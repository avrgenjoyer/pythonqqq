тест 1 
1) проверяем .is_empty == True 
2) записывает n элементов в стэк. Проверяем каждый раз .top()==n
3) проверяем .is_empty == False 
4) проверяем .size  == (n+1) (т.к цикл прошел от 0 до n-1)
5) используем .pop и идет обратно в цикле пока n не станет 0. Проверяем сопадает ли удаленный элемент с n
6) проверяем .size  == 0
7) используем еще несколько раз .pop и проверяем, что выдает None
8) проверяем .size  == 0

тест 2
1) записывает n элементов в стэк. Уже знаем, что это работает
2) проверяем .is_empty == False . Уже знаем, что это работает
3) используем .clear
4) проверяем .is_empty == True 

Тест 3 (критические случаи)
1) Если применять к пустому стеку .pop == None
2) Если применять к пустому стеку .top == None
3) Если применять к пустому стеку .size == 0